Optimize image  =====================================
https://jakearchibald.github.io/svgomg/
https://tinyjpg.com/ 

advance: 
    + responsive
    + size small
    + easy customize

Optimize in photoshop can obtain highest

in new version of create react app has strict mode but material ui is not support

================================= feature =======================

AppBar default position is FIXED

ToolBar to horizol item -> strength






--------------------- customize -------------------------

createMUiThemese is a instance 
    + passing a object -> override styling in application
    + palette -> manager color configuration in system -> can use to dark mode and light mode
        - all feature below to balance color in background
makeStyle to inline styling

can customize style in theme (ex: tab)

use onChange and value to set change for tab

fix when reload in browser reset to home to check


menu is built on top of other material ui component, can't add color to menu 
apply css to paper component in the menu component
set menu to elevation = 0 -> to unset box shadow